# OfferClaw 快速启动指南

## 项目现状

已完成基础架构搭建，包括：

- ✅ **Monorepo项目结构**（pnpm workspace）
- ✅ **Docker开发环境**（PostgreSQL + Redis）
- ✅ **后端服务**（FastAPI + SQLAlchemy + JWT认证）
- ✅ **浏览器扩展**（Chrome MV3 + TypeScript + Vite）
- ✅ **核心功能**：
  - Content Script：表单字段提取（无障碍树）
  - Content Script：表单填写执行（支持React/Vue）
  - Background Script：消息路由 + API调用
  - Popup UI：一键填表界面
  - 数据模型：Profile、Job、Application
  - API路由：画像管理、字段匹配、投递记录

---

## 快速启动（5分钟）

### 1. 安装依赖

```bash
# 安装pnpm（如果未安装）
npm install -g pnpm

# 安装项目依赖
pnpm install
```

### 2. 启动Docker服务

```bash
# 启动PostgreSQL + Redis
pnpm docker:up

# 等待服务启动（约10秒）
# 查看服务状态
docker ps
```

### 3. 配置环境变量

```bash
# 复制环境变量模板
cd packages/backend
cp .env.example .env

# 编辑.env文件，至少配置以下内容：
# OPENAI_API_KEY=sk-your-openai-api-key  # 必填
# SECRET_KEY=your-secret-key              # 必填
```

### 4. 启动后端服务

```bash
# 回到项目根目录
cd ../..

# 启动FastAPI服务
pnpm dev:backend

# 访问 http://localhost:8000/docs 查看API文档
# 访问 http://localhost:8000/health 测试健康检查
```

### 5. 启动浏览器扩展

```bash
# 启动扩展开发服务器
pnpm dev:extension

# 打开Chrome浏览器
# 访问 chrome://extensions
# 开启"开发者模式"
# 点击"加载已解压的扩展程序"
# 选择 packages/extension/dist 目录
```

### 6. 测试自动填表

1. 打开任意企业网申页面（如 https://www.nowcoder.com）
2. 点击浏览器右上角的OfferClaw图标
3. 点击"开始自动填表"
4. 查看检测结果和填写状态

---

## 目录结构

```
offerclaw/
├── packages/
│   ├── backend/              # ✅ FastAPI后端（已完成）
│   │   ├── app/
│   │   │   ├── api/          # API路由
│   │   │   ├── core/         # 核心（数据库、认证）
│   │   │   ├── models/       # 数据模型
│   │   │   ├── schemas/      # Pydantic schema
│   │   │   └── main.py       # FastAPI入口
│   │   ├── requirements.txt
│   │   └── .env.example
│   │
│   ├── extension/            # ✅ 浏览器扩展（已完成）
│   │   ├── src/
│   │   │   ├── background/   # Service Worker
│   │   │   ├── content/      # Content Script
│   │   │   ├── popup/        # Popup UI
│   │   │   └── manifest.json
│   │   ├── vite.config.ts
│   │   └── package.json
│   │
│   └── shared/               # ✅ 共享类型（已完成）
│       ├── types.ts          # TypeScript类型定义
│       └── package.json
│
├── docker/                   # ✅ Docker配置（已完成）
│   ├── docker-compose.yml    # PostgreSQL + Redis
│   └── Dockerfile.backend
│
├── package.json              # Monorepo根配置
├── pnpm-workspace.yaml       # Workspace配置
├── .gitignore
└── README.md
```

---

## 下一步开发任务

根据开发文档，接下来的任务顺序：

### 高优先级（核心功能）

1. **个人画像中枢（F1）** - Step 3
   - 实现简历PDF上传解析（使用pdfplumber + LLM）
   - 结构化画像生成与编辑
   - 敏感数据本地存储机制

2. **LLM字段语义匹配** - Step 4补充
   - 实现后端LLM匹配逻辑（OpenAI/Anthropic SDK）
   - 字段匹配精度优化
   - 置信度计算和低置信度处理

3. **简历定制引擎（F3）** - Step 5
   - JD关键词提取
   - 经历改写（不虚构）
   - SSE流式输出

4. **岗位雷达（F2）** - Step 7
   - 岗位数据抓取
   - 匹配度计算
   - 推荐排序

### 中优先级（辅助功能）

5. **投递看板（F5）** - Step 6
   - 投递记录自动创建
   - 状态流转和时间线
   - Web前端看板UI

6. **Web前端** - packages/webapp
   - Next.js 15 + TailwindCSS
   - 画像管理页面
   - 岗位雷达页面
   - 投递看板页面

### 低优先级（部署测试）

7. **集成联调与部署** - Step 8
   - 三端联调
   - 端到端测试
   - Docker生产部署
   - Chrome Web Store发布

---

## 技术栈确认

| 组件 | 技术 | 版本 | 状态 |
|---|---|---|---|
| 后端框架 | FastAPI | 0.111.0 | ✅ 已安装 |
| 数据库 | PostgreSQL | 16 | ✅ Docker运行 |
| 缓存 | Redis | 7.x | ✅ Docker运行 |
| ORM | SQLAlchemy | 2.0.31 | ✅ 已安装 |
| 认证 | JWT (python-jose) | 3.3.0 | ✅ 已安装 |
| PDF解析 | pdfplumber | 0.11.0 | ✅ 已安装 |
| LLM | OpenAI SDK | 1.37.0 | ✅ 已安装 |
| 扩展框架 | Chrome MV3 | - | ✅ 已配置 |
| 构建工具 | Vite | 5.3.4 | ✅ 已安装 |
| TypeScript | TypeScript | 5.5.3 | ✅ 已安装 |

---

## 开发建议

1. **先实现画像中枢**：这是所有功能的数据基础
2. **完善LLM匹配**：这是自动填表的核心能力
3. **测试真实场景**：打开企业网申页面测试效果
4. **隐私优先**：敏感数据永远只在浏览器本地
5. **渐进式开发**：先MVP核心功能，再扩展辅助功能

---

## 常见问题

### Q: Docker服务启动失败？
```bash
# 检查Docker是否运行
docker info

# 重启Docker服务
docker-compose down
docker-compose up -d
```

### Q: 扩展无法加载？
```bash
# 确认dist目录存在
ls packages/extension/dist

# 如果不存在，先构建
pnpm build:extension
```

### Q: API返回401错误？
```bash
# 检查是否配置了SECRET_KEY
cat packages/backend/.env | grep SECRET_KEY

# 检查Token是否存在
# 在扩展Popup中查看"未登录"状态
```

### Q: 表单字段无法提取？
- 确认页面是HTTPS（扩展只在HTTPS页面运行）
- 检查Content Script是否加载（查看Console日志）
- 尝试刷新页面后重新检测

---

## 联系方式

如有问题，请参考：
- [开发文档.md](../开发文档.md) - 完整技术文档
- [README.md](../README.md) - 项目说明

---

**祝开发顺利！OfferClaw将帮助万千校招求职者提升效率。**